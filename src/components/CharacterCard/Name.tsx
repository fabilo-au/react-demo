import { Typography, styled } from "@mui/material";

const Name = styled(Typography)(() => ({
  textAlign: "center",
  position: "absolute",
  bottom: 0,
  left: 0,
  right: 0,
}));

export default Name;
